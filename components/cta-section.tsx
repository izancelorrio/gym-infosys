import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"
import { Check, Gift } from "lucide-react"

export function CTASection() {
  // CTA removed per request (frame content cleared)
  return null
}
