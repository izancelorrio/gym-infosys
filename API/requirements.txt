# Dependencias para el API (main.py)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Dependencias para autenticación y hashing
bcrypt==4.1.2

# Dependencias para hacer requests HTTP
requests==2.31.0

# Dependencias adicionales que pueden ser útiles
python-jose[cryptography]==3.3.0  # Para JWT tokens
passlib[bcrypt]==1.7.4  # Para hashing de passwords
email-validator==2.1.0  # Para validar emails

# Base de datos (SQLite está incluido en Python por defecto)
# sqlite3 viene incluido con Python

# Para desarrollo y testing (opcional)
pytest==7.4.3
httpx==0.25.2  # Para testing de FastAPI